# 부스 안내 가이드 웹사이트 — 제작 제안서

> 2026 경북 초등 수업나눔축제 · 수다방 부스
> 원본: `26경북_초등_수업나눔축제_부스안내가이드.md`

---

## 1. 목표

부스 방문 교사가 **QR로 접속 → 30초 안에 무엇을 체험할 수 있는지 파악 → 웹앱으로 바로 이동**할 수 있는
싱글 페이지 안내 사이트. 현장에서는 대부분 **스마트폰 세로 화면**으로 볼 것을 전제로 설계.

## 2. 기술 스택 / 산출물

| 항목 | 결정 |
|---|---|
| 형태 | **단일 HTML 파일** (`index.html`) — CSS/JS 인라인, 외부 의존성 0 |
| 이유 | Netlify/Pages/GitHub Pages 어디든 파일 하나만 올리면 끝, 오프라인에서도 동작 |
| 폰트 | 시스템 폰트 스택 (Apple SD Gothic Neo → Pretendard → -apple-system) |
| 효과음 | **Web Audio API로 코드 생성** (음원 파일 없음, 용량 0) |
| 브라우저 | iOS Safari / Android Chrome / 데스크톱 크롬 |

## 3. 페이지 구조 (스크롤 1페이지 + 앵커 네비)

```
① Hero            테마 문구 + "수다방" + 스크롤 유도
② 부스 개요        대상 / 포인트 / 준비물  (카드 3장)
③ 체험 동선        테이블1 ➔ 테이블2 시각 플로우
④ 테이블 1 : 수학&코딩체험존
     - 태그 칩 (#바이브코딩 #제미나이캔버스 #수학올림픽)
     - 전시/안내 (안내영상 링크)
     - 체험 웹앱 4종 → 큰 링크 카드 (탭하면 새 창)
     - 체험 코너 1·2·3 (아코디언 or 카드)
⑤ 테이블 2 : 수학&동화제작소
     - 태그 칩 (#수학동화 #제미나이노트북 #캐릭터생성)
     - 전시 코너 (드라이브 폴더 / 안내영상)
     - PC 버전 : Step 1 → Step 2
     - 스마트폰 버전 : 6단계 스텝 타임라인
     - ⚠️ 노트북 3회 후 계정 전환 주의 배너
⑥ 공유 플랫폼      패드렛 QR / 프롬프트 카드 안내
⑦ Footer          연구회명 + 맨 위로
```

상단에 **고정 세그먼트 컨트롤**(개요 · 테이블1 · 테이블2 · 링크)을 두어 iOS 스타일로 섹션 이동.

## 4. 디자인 방향 (애플 감성)

- **여백 위주 레이아웃** — 섹션당 세로 여백 크게, 텍스트 밀도 낮게
- **타이포 중심** — 헤드라인 굵고 크게(-0.03em 자간), 본문은 회색 계열 secondary
- **재질감** — `backdrop-filter: blur()` 유리 네비바, 카드 radius 20~28px, 아주 옅은 그림자
- **컬러** — 뉴트럴 배경(#f5f5f7 / #000) + 포인트 컬러 2개
  - 테이블1 = 블루~시안 그라데이션, 테이블2 = 퍼플~핑크 그라데이션
- **모션** — 스크롤 진입 시 `IntersectionObserver`로 fade-up (translateY 16px, 0.6s ease-out),
  카드 탭 시 `scale(0.97)` 눌림 피드백
- **다크모드** — `prefers-color-scheme` 자동 대응
- **접근성** — `prefers-reduced-motion` 존중, 탭 타겟 최소 44px

## 5. 반응형 기준

| 폭 | 레이아웃 |
|---|---|
| ~600px (스마트폰) | 1단, 카드 풀폭, 히어로 폰트 clamp 축소 |
| 600~1024px (태블릿) | 2단 그리드 |
| 1024px~ (모니터·현장 전시용) | 최대폭 1080px 중앙 정렬, 3단 그리드 |

`clamp()` 기반 유동 타이포 + CSS Grid `auto-fit`으로 브레이크포인트 최소화.

## 6. 효과음 (간단)

Web Audio API 오실레이터로 짧은 톤 생성. **음원 파일 없음.**

| 트리거 | 소리 |
|---|---|
| 탭/버튼 클릭 | 짧은 "톡" (사인파 880Hz, 60ms) |
| 섹션 이동 | 상행 2음 (660→880Hz) |
| 외부 링크 열기 | 밝은 3음 아르페지오 |

- 우측 하단 **🔊 / 🔇 토글 버튼** 제공, 설정은 `localStorage` 저장
- iOS 정책상 **첫 사용자 탭에서 AudioContext 활성화**
- 볼륨 기본 0.15로 아주 작게 (현장 소음 고려, 거슬리지 않게)

## 7. 확인 필요 — 이미지 에셋

원본 md가 참조하는 `./assets/` 폴더(썸네일1.png, 체험존.png, 제작소.png, 6·7·8·9·10·11.png)가
`~/Downloads`에 **없습니다.**

→ 제안: 일단 **CSS 그라데이션 + 이모지 아이콘 플레이스홀더**로 만들고,
   나중에 `assets/` 폴더를 프로젝트에 넣으면 자동으로 이미지가 뜨도록
   `onerror` 폴백 처리해두겠습니다. (에셋 주시면 바로 반영 가능)

## 8. 그 외 제안 (원하시면 포함)

- [ ] 각 웹앱 링크에 **QR 코드** 자동 생성 (현장에서 화면 보여주고 바로 스캔 — JS로 그림)
- [ ] "체험 완료 스탬프" 인터랙션 (탭하면 체크 표시, localStorage 저장)
- [ ] 인쇄용 스타일 (A4 한 장 안내문으로 출력)

---

## 검토 부탁드립니다

1. **구조**(4~6번 섹션 순서·구성) 이대로 괜찮은지
2. **컬러 포인트** — 블루/퍼플 조합 vs 다른 톤 선호
3. **이미지 에셋** — 보내주실지 / 플레이스홀더로 갈지
4. **8번 추가 기능** 중 넣을 것
5. 배포 계획 (Netlify? GitHub Pages? 로컬 파일만?)

피드백 주시면 그대로 `index.html` 작성하겠습니다.
