# 리디자인 제안 — "보드게임 파티" 감성으로

> 참고: 마리오파티 잼버리 블로그 리뷰, 닌텐도 코리아 공식 페이지
> 방향: 아기자기하고 귀엽게, 지금의 "3D 눌림 버튼" 골격은 유지하고 그 위에 파티 게임 감성을 입힘

---

## 0. 저작권 관련 원칙 (먼저 확정하고 싶은 부분)

참고 링크의 **마리오·닌텐도 캐릭터·로고·서체는 그대로 쓰지 않습니다.** 대신 그 페이지들이 주는 **무드**만 가져옵니다 —
알록달록 원색, 통통 튄 굵은 글자, 별·주사위·리본 같은 파티 소품, 사선 캔디스트라이프 배경.
실제 이미지 자산은 지금 쓰고 있는 우리 손그림 캐릭터(수학&코딩체험존/동화제작소 배너 속 아이들·로봇·여우)를
계속 재사용합니다. 이 원칙에 이견 없으신지 먼저 확인 부탁드립니다.

---

## 1. 배경 — 사선 캔디스트라이프

지금은 섹션 전체가 단색(`--bg`)입니다. 마리오파티 페이지처럼 **아주 옅은 사선 줄무늬**를 배경에 깔아 파티장 느낌을 줍니다.

```
연한 하늘색/크림색 두 톤을 45° 대각선 stripe로, 존별 포인트컬러 살짝 섞어서
(테이블1=블루 계열, 테이블2=퍼플 계열, 랜딩=중립)
```
- CSS `repeating-linear-gradient`만으로 구현 (이미지 파일 불필요)
- 너무 튀지 않게 명도 차이를 작게 — 배경이 텍스트를 방해하면 안 되니 채도·대비는 낮게

## 2. 타이틀 — "게임 로고" 스타일 스티커 텍스트

`h2`(수학&코딩체험존, 수학&동화제작소, 부스 개요 등 큰 제목)에 **흰 테두리 + 그림자**를 입혀서
게임 로고처럼 보이게 합니다. 폰트 교체 없이 `text-shadow` 다중 레이어로 구현합니다.

```
색이 있는 글자 + 흰색 아웃라인 4~6방향 + 아래쪽 컬러 그림자 한 겹
→ 지금 배너 이미지 속 손글씨체 로고("수학&코딩체험존" 등)와 톤이 맞음
```

## 3. 장식 모티프 — 별·반짝임·리본 배지

- 카드 모서리, 섹션 제목 옆에 ⭐✨🎲 같은 작은 장식을 CSS로 은은하게 배치 (지금 `.hero`에 있는 blur 원 장식을 대체/보강)
- "체험존 전체 안내", "PC 버전 체험" 같은 안내판 카드 중 대표 카드에 **리본 모양 배지**("BEST", "START" 등) 하나씩 — 과하지 않게 1~2곳만
- 로딩/등장 애니메이션에 살짝 **바운스**(overshoot easing)를 섞어 통통 튀는 느낌 강화 (`cubic-bezier`를 지금의 부드러운 ease-out에서 약간 튀는 값으로)

## 4. 버튼 — 지금의 3D 눌림 버튼을 "파티 타일"로

지난번에 만든 눌리는 입체 버튼(테두리+아랫단 그림자) 골격은 그대로 두고:
- 배경을 단색보다 **파스텔↔비비드 그라데이션**으로 (지금보다 채도를 조금 더 올림)
- hover 시 은은한 **반짝임(shine) 스윕** 한 번 지나가는 효과 (주사위 눌렀을 때 반짝하는 느낌)
- 아이콘 배지를 사각형보다 **말풍선/타일 모양**에 가깝게 라운드 값 조정

## 5. 섹션 제목 — 리본/현수막 바

"Table 1 · 수학&코딩체험존" 같은 소제목 라인을 파티장 현수막처럼 **양끝이 제비꼬리(pennant) 모양**인
작은 리본 바 배경으로. 순수 CSS `clip-path`로 구현, 이미지 불필요.

## 6. 마스코트 재등장

이미 갖고 있는 배너 속 캐릭터(연필 든 아이, 로봇, 여우 등)를 **작게 잘라서** 사운드 토글 버튼 옆이나
푸터에 "가이드 캐릭터"처럼 다시 배치 — 새 이미지 제작 없이 기존 `img/` 재사용.

---

## 범위 밖으로 두는 것 (과하면 산만해지므로)

- 배경음악 자동재생, 캐릭터 음성 — 현장 정숙 고려해 배제
- 움직이는 GIF/스프라이트 애니메이션 — 로딩 무거워짐, CSS 애니메이션으로 충분
- 폰트 교체(둥근 게임체 등) — 외부 폰트 로드는 오프라인 배포 원칙에 안 맞아 시스템 폰트 유지, 대신 스티커 텍스트 효과로 보완

---

## 확인 부탁드립니다

1. **0번 저작권 원칙** — 마리오 IP 요소(캐릭터·로고) 없이 무드만 차용하는 방향 괜찮으신지
2. **1~6번 중 우선순위** — 전부 반영할지, 일부만(예: 배경 스트라이프 + 스티커 타이틀만 먼저) 먼저 볼지
3. **톤 강도** — "은은하게" vs "확실하게 파티 느낌 나게" 중 어느 쪽에 가깝게 갈지

피드백 주시면 반영해서 코드로 옮기겠습니다.
